strike(number){
  switch(number){
    case number <= length:
    case number > 1:
    case !striked.includes(number):
      striked.push(number)
      break
  }
  if(number <= length  and number > 1){

  }
}